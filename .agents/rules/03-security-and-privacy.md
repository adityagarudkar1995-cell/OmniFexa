# Security and Privacy

- No secrets in code or chat
- Use `.env.local` for local secrets, `.env.example` for placeholders
- Sensitive files must not be permanently stored
- Browser-capable processing stays on device
- Server processing only when technically required
- No real user data in tests or examples
